buildscript {
    dependencies {
        classpath fileTree(dir: 'libs', include: '*.jar')
    }
}

apply {
    plugin 'com.nexusunsky.CustomPlugin'
}

hello {
    message = 'hello gradle plugin task :ex55GroovyProject'
}